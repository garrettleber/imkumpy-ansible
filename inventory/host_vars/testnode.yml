---
# roles/docker_deployment
service_account: lab
docker_apps: ["omada-controller"]

# roles/common_debian
iptables_custom_rules:
  - comment: HTTP
    port: "80"
    protocol: tcp
  - comment: DNS
    port: "53"
    protocol: tcp
  - comment: DNS
    port: "53"
    protocol: udp
