---
- name: Setup Docker container runtime and applications
  hosts: dockerhost,gitea,omada,prod-mc,qbittorrent,rp
  become: true
  tags: docker-deployment
  roles:
    - role: geerlingguy.docker
      vars:
        docker_install_compose: true
    - role: docker_deployment
